# Faça um programa que calcule todos os numeros impares que são mutiplos de 3 entre 1 a 500

valor = 0

for c in range(0, 500):
    if c % 3 == 0:
        total += c
        cont = + 1

print(f'A soma de todos os {cont} valores solicitados é {total}')
        